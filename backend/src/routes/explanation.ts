import {Hono} from "hono";import {explainSelectedText} from "../services/gemini";
const explanation=new Hono();explanation.post("/",async c=>{const body=await c.req.json<unknown>().catch(()=>null);if(!body||typeof body!=="object")return c.json({error:{message:"Invalid JSON body",code:"INVALID_INPUT"}},400);const b=body as {selectedText?:unknown;documentTitle?:unknown;pageNumber?:unknown};if(typeof b.selectedText!=="string"||b.selectedText.trim().length<3||b.selectedText.length>12000||b.documentTitle!==undefined&&typeof b.documentTitle!=="string"||b.pageNumber!==undefined&&(!Number.isInteger(b.pageNumber)||Number(b.pageNumber)<1))return c.json({error:{message:"Selected text must be 3–12000 characters",code:"INVALID_INPUT"}},400);return c.json({explanation:await explainSelectedText({selectedText:b.selectedText.trim(),documentTitle:b.documentTitle as string|undefined,pageNumber:b.pageNumber as number|undefined})})});export default explanation;
