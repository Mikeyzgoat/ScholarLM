import {useState} from "react";import {useNavigate,useParams} from "react-router-dom";import {useQuery} from "@tanstack/react-query";import type {Editor} from "tldraw";import {getNote,updateNote} from "../services/notes";import {NotesCanvas} from "../components/notes/NotesCanvas";import {NotesHeader} from "../components/notes/NotesHeader";
export default function NotesPage(){const{noteId=""}=useParams();const nav=useNavigate();const q=useQuery({queryKey:["note",noteId],queryFn:()=>getNote(noteId),enabled:!!noteId});const[editor,setEditor]=useState<Editor|null>(null);void editor;if(q.isLoading)return <main className="p-6">Loading note…</main>;if(q.isError||!q.data)return <main className="p-6 text-red-700">Unable to load note.</main>;return <main className="fixed inset-0 bg-white"><NotesHeader title={q.data.title} saveState="saved" onTitleChange={title=>void updateNote({noteId,title,expectedRevision:q.data.revision}).then(()=>q.refetch())} onBack={()=>nav(`/workspace/${q.data.documentId}`)}/><NotesCanvas note={q.data} onEditorReady={setEditor}/></main>}
