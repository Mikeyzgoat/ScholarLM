import {useState} from "react";import {useMutation} from "@tanstack/react-query";import {searchDocument} from "../services/search";import type {SearchResult} from "../lib/types";
export function useSemanticSearch(documentId:string){const[query,setQuery]=useState("");const[results,setResults]=useState<SearchResult[]>([]);const mutation=useMutation({mutationFn:(q:string)=>searchDocument({documentId,query:q}),onSuccess:setResults});return{query,setQuery,results,search:()=>{const q=query.trim();if(q)mutation.mutate(q)},isSearching:mutation.isPending,error:mutation.error,clear:()=>{setQuery("");setResults([]);mutation.reset()}}}
