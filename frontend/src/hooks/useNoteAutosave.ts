import {useEffect,useRef,useState} from "react";import {getSnapshot,type Editor} from "tldraw";import {NOTE_AUTOSAVE_DELAY} from "../lib/constants";import type {NotePage,SaveState} from "../lib/types";import {removeLocalNoteDraft,saveLocalNoteDraft} from "../lib/noteStorage";import {updateNote} from "../services/notes";
export function useNoteAutosave({note,editor,onServerNoteUpdated}:{note:NotePage|undefined;editor:Editor|null;onServerNoteUpdated?:(note:NotePage)=>void}){const[saveState,setSaveState]=useState<SaveState>("saved"),[lastSavedAt,setLastSavedAt]=useState<string|null>(note?.updatedAt??null);const timer=useRef<ReturnType<typeof setTimeout>|null>(null),revision=useRef(note?.revision??1),applying=useRef(true);useEffect(()=>{revision.current=note?.revision??1;applying.current=true;const id=requestAnimationFrame(()=>{applying.current=false});return()=>cancelAnimationFrame(id)},[note?.id]);useEffect(()=>{if(!note||!editor)return;const unsubscribe=editor.store.listen(()=>{if(applying.current)return;const snapshot=getSnapshot(editor.store),updatedAt=new Date().toISOString();saveLocalNoteDraft({noteId:note.id,snapshot,metadata:note.metadata,revision:revision.current,updatedAt});setSaveState("unsaved");if(timer.current)clearTimeout(timer.current);timer.current=setTimeout(async()=>{setSaveState("saving");try{const updated=await updateNote({noteId:note.id,snapshot,metadata:note.metadata,expectedRevision:revision.current});revision.current=updated.revision;removeLocalNoteDraft(note.id);setLastSavedAt(updated.updatedAt);setSaveState("saved");onServerNoteUpdated?.(updated)}catch{setSaveState("error")}},NOTE_AUTOSAVE_DELAY)},{scope:"document"});return()=>{unsubscribe();if(timer.current)clearTimeout(timer.current)}},[editor,note?.id]);return{saveState,lastSavedAt,recoverableDraftFound:note?Boolean(localStorage.getItem(`scholarlm-note-draft:${note.id}`)):false}}
