import {useExplanation} from "../../hooks/useExplanation";import {SelectionPopover} from "../pdf/SelectionPopover";import {ExplanationContent} from "./ExplanationContent";
export function ExplainPanel({selectedText,pageNumber,documentTitle}:{selectedText:string;pageNumber:number|null;documentTitle:string}){const state=useExplanation();return <section className="space-y-3 rounded-lg border bg-white p-4"><h2 className="font-semibold">Explanation</h2>{selectedText&&!state.explanation&&<SelectionPopover selectedText={selectedText} onExplain={()=>void state.explain({selectedText,documentTitle,pageNumber:pageNumber??undefined})} onDismiss={state.clear}/>}<ExplanationContent selectedText={selectedText} explanation={state.explanation} isLoading={state.isExplaining} error={state.error}/></section>}
