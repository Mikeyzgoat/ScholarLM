export function ExplanationContent({selectedText,explanation,isLoading,error}:{selectedText:string;explanation:string;isLoading:boolean;error:Error|null}){if(isLoading)return <p className="text-sm">Explaining selection…</p>;if(error)return <p className="text-sm text-red-700">{error.message}</p>;if(explanation)return <div className="space-y-3 whitespace-pre-wrap text-sm leading-6"><p className="border-l-2 pl-3 text-xs text-stone-500">{selectedText}</p>{explanation}</div>;return <p className="text-sm text-stone-500">Select text and request an explanation.</p>}
