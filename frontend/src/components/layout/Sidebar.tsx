import {useQuery} from "@tanstack/react-query";import {BookOpen,Home} from "lucide-react";import {Link,useParams} from "react-router-dom";import {listDocuments} from "../../services/documents";
export function Sidebar(){const{id}=useParams();const q=useQuery({queryKey:["documents"],queryFn:listDocuments});return <aside className="hidden w-56 shrink-0 border-r bg-stone-900 p-4 text-stone-200 lg:block"><Link to="/" className="mb-8 flex items-center gap-2 font-semibold text-white"><BookOpen/>ScholarLM</Link><Link to="/" className="flex gap-2 rounded p-2 hover:bg-stone-800"><Home size={18}/>Home</Link><p className="mb-2 mt-7 text-xs uppercase text-stone-500">Recent</p>{q.data?.slice(0,6).map(d=><Link key={d.id} to={`/workspace/${d.id}`} className={`block truncate rounded p-2 text-sm ${id===d.id?"bg-stone-700 text-white":"hover:bg-stone-800"}`}>{d.name}</Link>)}</aside>}
