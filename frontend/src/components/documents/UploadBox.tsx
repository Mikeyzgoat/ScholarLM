import {useRef,useState} from "react";import {Upload} from "lucide-react";import {uploadDocument} from "../../services/documents";import type {DocumentSummary} from "../../lib/types";
export function UploadBox({onUploaded}:{onUploaded:(document:DocumentSummary)=>void}){const ref=useRef<HTMLInputElement>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function accept(file?:File){setError("");if(!file)return;if(file.size>50*1024*1024){setError("PDF must be 50 MB or smaller.");return}if(file.type!=="application/pdf"&&!file.name.toLowerCase().endsWith(".pdf")){setError("Choose a valid PDF file.");return}setBusy(true);try{onUploaded(await uploadDocument(file))}catch(e){setError(e instanceof Error?e.message:"Upload failed")}finally{setBusy(false)}}return <section className="rounded-xl border-2 border-dashed border-stone-300 bg-white p-10 text-center" onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();void accept(e.dataTransfer.files[0])}}><Upload className="mx-auto mb-3 text-teal-700"/><h2 className="font-semibold">Upload a research PDF</h2><p className="mt-1 text-sm text-stone-500">PDF, up to 50 MB</p><button disabled={busy} onClick={()=>ref.current?.click()} className="mt-5 rounded-lg bg-teal-700 px-4 py-2 text-white focus:outline-2">{busy?"Uploading…":"Choose PDF"}</button><input ref={ref} hidden type="file" accept="application/pdf,.pdf" onChange={e=>void accept(e.target.files?.[0])}/>{error&&<p className="mt-3 text-sm text-red-700">{error}</p>}</section>}
