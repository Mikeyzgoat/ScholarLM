import type {SearchResult} from "../../lib/types";
export function SearchResults({results,onSelectResult,isLoading}:{results:SearchResult[];onSelectResult:(r:SearchResult)=>void;isLoading:boolean}){if(isLoading)return <p className="py-4 text-sm">Searching…</p>;if(!results.length)return <p className="py-4 text-sm text-stone-500">No search results.</p>;return <div className="space-y-2">{results.map(r=><button key={r.chunkId} onClick={()=>onSelectResult(r)} className="w-full rounded-lg border bg-white p-3 text-left hover:border-teal-600"><span className="text-xs font-semibold text-teal-700">Page {r.pageNumber} · {(r.score*100).toFixed(0)}%</span><p className="mt-1 line-clamp-4 text-sm text-stone-700">{r.content}</p></button>)}</div>}
